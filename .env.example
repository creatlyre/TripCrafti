# Public Supabase client credentials (safe to expose to browser)
PUBLIC_SUPABASE_URL=https://cyusoliinkrsrtxgtbbl.supabase.co
PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_ANON_KEY

# Legacy variable names (fallback). You can keep these if already used in deployment.
# SUPABASE_URL=###
# SUPABASE_KEY=###

# Other public API keys
OPENROUTER_API_KEY=###

# Never commit service role keys (service_role) to version control.
# If you need server-side Supabase operations later, add SERVICE_ROLE_KEY only in runtime secrets (not checked in).