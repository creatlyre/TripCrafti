###############################################
# TripCrafti Example Environment Configuration #
###############################################

# Copy this file to .env and fill the required values.
# Values prefixed with PUBLIC_ are exposed to the browser bundle.

################################################
# 1. Supabase (Required)
################################################
# Public Supabase client credentials (safe to expose to browser)
PUBLIC_SUPABASE_URL=https://cyusoliinkrsrtxgtbbl.supabase.co
PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_ANON_KEY

# Legacy variable names (fallback). Only needed if some deployment scripts still rely on them.
# SUPABASE_URL=
# SUPABASE_KEY=

################################################
# 2. AI Itinerary Assistant (Gemini / Google AI Studio)
################################################
# Required for generating itineraries via /api/trips/[tripId]/itinerary
GEMINI_API_KEY=
# Optional override of the auto-selected model (e.g. gemini-1.5-flash, gemini-1.5-pro)
# GEMINI_MODEL=gemini-1.5-flash

################################################
# 3. Destination Images (Unsplash)
################################################
# Required for /api/images/[destination] endpoint for illustrative trip images
UNSPLASH_ACCESS_KEY=

################################################
# 4. Foreign Exchange Rates
################################################
# Base URL for FX API. Defaults internally to https://api.exchangerate.host if unset.
# Keep PUBLIC_ prefix if client components might need to display or prefetch rate info.
PUBLIC_FX_API_BASE=https://api.exchangerate.host

################################################
# 5. (Optional / Legacy / Future Keys)
################################################
# Currently unused placeholder; keep optional for future multi-provider LLM routing
# OPENROUTER_API_KEY=

################################################
# 6. Event & Festival Integration (PredictHQ)
################################################
# Required for fetching local events in the itinerary view
PREDICTHQ_ACCESS_TOKEN=

################################################
# 7. Geocoding (geocode.maps.co)
################################################
# Required for converting destination names to coordinates for event searching
GEOCODE_MAPS_CO_API_KEY=

################################################
# Notes & Security
################################################
# - Never commit real secrets (.env) to version control.
# - Do NOT put Supabase service_role key here; configure it only in server deployment secrets if needed.
# - Restart the dev server after modifying environment variables.
# - PUBLIC_* prefix = accessible in client bundle; omit prefix for server-only secrets.
